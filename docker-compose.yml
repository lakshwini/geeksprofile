services:
  flask-app:
    build: .
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=env.local
      - SECRET_KEY=secret
      - MYSQL_HOST=192.168.1.162  # IP directe de votre Mac
      - MYSQL_USER=root
      - MYSQL_PASSWORD=
      - MYSQL_DB=geekprofile
      - MYSQL_PORT=3306
    restart: unless-stopped