services:
  flask-app:
    build: .
    ports:
      - "5001:5000"
    environment:
      - MYSQL_HOST=host.docker.internal
      - MYSQL_USER=root
      - MYSQL_PASSWORD=ton_mdp_mysql
      - MYSQL_DB=geekprofile