services:
  flask-app:
    build: .
    ports:
      - "5001:5000"
    environment:
      - MYSQL_HOST=10.10.107.83  # IP Mac
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
      - MYSQL_DB=geekprofile
      - MYSQL_PORT=3306
    